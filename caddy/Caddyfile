# HTTP on port 8080, HTTPS on port 8443
# Site-specific configs live in /etc/caddy/sites/ (mounted from caddy/sites/)

# HTTP server - redirect all to HTTPS
http://localhost:8080 {
    redir https://localhost:8443{uri}
}

# Import per-site configuration files
import /etc/caddy/sites/*.conf
